name: PetRecovery
options:
  bundleIdPrefix: com.petrecovery
  deploymentTarget:
    iOS: "16.0"
  createIntermediateGroups: true

configs:
  Debug: debug
  Release: release

packages:
  Stripe:
    url: https://github.com/stripe/stripe-ios-spm
    from: 23.18.0

targets:
  PetRecovery:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - path: App
      - path: Models
      - path: Services
      - path: Views
    info:
      path: Info.plist
      properties:
        UILaunchScreen: {}
        CFBundleDisplayName: PetRecovery
        NSCameraUsageDescription: PetRecovery uses the camera to scan QR tags and add pet photos.
        NSLocationWhenInUseUsageDescription: PetRecovery uses your location to search for lost pets and verify reunions.
        NSPhotoLibraryUsageDescription: PetRecovery uses your photo library so you can upload pet profile photos.
        NSLocationTemporaryUsageDescriptionDictionary:
          RewardProximityVerification: Full-accuracy location is needed to confirm you and the finder are close enough to release the reward.
        CFBundleURLTypes:
          - CFBundleURLName: app.petrecovery.deeplink
            CFBundleURLSchemes:
              - petrecovery
    entitlements:
      path: PetRecovery.entitlements
      properties:
        aps-environment: development
        keychain-access-groups:
          - $(AppIdentifierPrefix)com.petrecovery.app
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.petrecovery.app
        SWIFT_VERSION: "5.9"
        TARGETED_DEVICE_FAMILY: "1"
        IPHONEOS_DEPLOYMENT_TARGET: "16.0"
    dependencies:
      # Adjust the product name to whichever Stripe module you actually
      # use — StripePaymentSheet covers the escrow/reward payment flow;
      # swap or add StripePayments / StripeApplePay if needed.
      - package: Stripe
        product: StripePaymentSheet
    capabilities:
      - PushNotifications
      - InAppPurchase
      - Keychain

  PetRecoveryTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - path: PetRecoveryTests
    dependencies:
      - target: PetRecovery

schemes:
  PetRecovery:
    build:
      targets:
        PetRecovery: all
        PetRecoveryTests: [test]
    test:
      targets:
        - PetRecoveryTests
    run:
      config: Debug
    archive:
      config: Release
